// file path + modified date + size + list of cached files (diff types)
// consider having more cached files together
// relationships between files
// used to check if the unit needs to be recompiled