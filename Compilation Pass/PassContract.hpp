//! defines the passes on which it depends
//! defines the analyses that it requires, or it would like to have (available only for max opt level, for ex)
//! defines the analyses that it preserves

//? specifies if it's an analyses or optimization
//? specifies if it only collects information (read-only)